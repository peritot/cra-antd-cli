const Path = Object.freeze({
  temp: '.temp',
});

const TemplateEnum = Object.freeze({
  react: {
    repository: {
      type: 'github',
      origin: 'github.com',
      owner: 'peritot',
      name: 'cra-antd-template',
    },
  },
});

module.exports = {
  Path,
  TemplateEnum,
};
